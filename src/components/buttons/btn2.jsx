
const Btn2 = ({iconIng,altText,className}) => {
    return( 
    <div className={className}>
       <button> <img src={iconIng} alt={altText}/>Nueva Parcela</button>
    </div>

    )
}

export default Btn2 